name: 🚀 Deploy website
on:
  push:
    branches: [ main ]
jobs:
  web-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Get latest code
        uses: actions/checkout@v3
      
      - name: 📦 Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: wp1108201.server-he.de
          username: ftp1108201-167027
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps
          port: 21
          security: loose  # Changed to loose to handle certificate issues
          timeout: 60000
          server-dir: ./
          local-dir: ./
